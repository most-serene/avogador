name: PR CICD pipeline
run-name: PR pipeline for ${{ github.ref }}
on:
  workflow_dispatch:
  pull_request:
jobs:
  Users:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Set up WireGuard
        uses: egor-tensin/setup-wireguard@v1
        with:
          endpoint: '${{ secrets.ENDPOINT }}'
          endpoint_public_key: '${{ secrets.ENDPOINT_PUBLIC }}'
          ips: '${{ secrets.IPS }}'
          allowed_ips: '${{ secrets.ALLOWED_IPS }}'
          private_key: '${{ secrets.PRIVATE }}'
        
      - name: testing vpn link
        run: |
          ping -c 1 dev-server.sanve.mostserene.eu
          
      - name: Download application.properties
        run: |
          mkdir -p backend/services/userservice/src/test/resources/
          curl -k https://repository.mostserene.eu/avogador/envs/testing/users/application.properties -u ${{ secrets.REPOSITORY_CREDENTIALS }}  --output backend/services/userservice/src/test/resources/application.properties

      - name: Gradle build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: assemble
          build-root-directory: backend/services/userservice
      
      - uses: actions/upload-artifact@v3
        with:
          name: usersService.jar
          path: backend/services/userservice/build/libs/*.jar
      
      - name: Gradle test
        uses: gradle/gradle-build-action@v2
        with:
          arguments: test
          build-root-directory: backend/services/userservice

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v4
        with:
          report_paths: '**/build/test-results/test/TEST-*.xml'
